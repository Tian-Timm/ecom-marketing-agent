# 当前运行与评测证据

本页记录 2026-08-01 本阶段重新生成的本地证据。证据所属阶段起始提交为
`707190f`；本阶段没有重新验证 Vercel Deployment ID，因此不沿用历史部署编号。

## 1. 版本与数据来源

- **流水线版本**：`2026-08-01.2`
- **风控规则版本**：`2026-07-30.1`
- **静态快照**：`generated_output/pipeline_result.json`，来自仓库内 10 条固定评测任务；只生成本地临时/输出图片，不访问飞书写接口。
- **公开实时模式**：`/api/demo_run` 调用 `run_public_demo_task(task_id)`，重新读取固定演示 Base，重新执行规则与 DeepSeek，返回 `execution_mode=live_readonly` 和 `writeback=false`。
- **管理员模式**：`/api/run`、`/api/sync` 保留管理员 Token 鉴权、飞书上传/回写和输入指纹幂等；同步单次最多 3 条任务。

## 2. 自动化测试

执行：

```text
python -m unittest discover -s tests -p "test_*.py"
python -m unittest discover -s .agents/skills/simple-visual-compliance/tests -p "test_*.py"
```

- 根目录测试：**49/49** 通过。
- 合规 Skill 测试：**36/36** 通过。
- 合计：**85/85** 通过。

## 3. 固定评测集

执行：

```text
python .agents/skills/simple-visual-compliance/scripts/evaluate_fixture.py
```

固定评测集共 10 条，其中 6 条预期为异常任务：

- **任务状态一致率**：10/10（100%）
- **异常任务检出率**：6/6（100%）
- **规则码一致率**：10/10（100%）
- **生成图片**：4 条合规任务
- **规则版本**：`2026-07-30.1`

评测真值仍来自现有 `evaluation_ground_truth.json`，本阶段没有修改评测数据。

## 4. 运行证据边界

`public_evidence.json` 中的耗时来自本次静态快照生成；它不是飞书或 Vercel 的在线端到端耗时。
公开实时运行每次都重新执行，不使用管理员的 `SKIPPED_UNCHANGED` 幂等跳过；管理员运行仍保留该能力。

当前本地环境没有 `DEEPSEEK_API_KEY`、飞书应用凭据或管理员 Token，因此本阶段未自动生成新的真实 DeepSeek/飞书写入证据。
这不改变线上代码路径，但不能据此声称本地完成了在线调用。

## 5. 已知限制

1. 固定评测集只有 10 条样本，不能代表全部行业法规或业务场景。
2. 公开实时运行依赖部署环境配置固定演示 Base 和 DeepSeek；未配置时 API 会返回脱敏的不可用错误。
3. 公开运行不上传图片、不回写飞书；管理员接口才拥有受保护的交付能力。
4. 公开 Demo 默认同一 IP 10 分钟最多 8 次，响应超限时包含 `Retry-After`；管理员接口限流口径保持独立。
5. 本阶段没有可自动确认的最新 Vercel Deployment ID，历史编号不作为当前发布证据。
